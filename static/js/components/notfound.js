function notfound() {
    return `
        <section>
            <p>Command not found!
            <p><span>Type 'help' + <kbd>Enter</kbd> -- to see available commands.</span></p>
        </section>
    `;
}